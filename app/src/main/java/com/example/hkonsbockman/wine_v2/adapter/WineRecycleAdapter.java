package com.example.hkonsbockman.wine_v2.adapter;

import android.support.v7.widget.RecyclerView;
import android.view.View;
import android.view.ViewGroup;
import android.widget.ImageView;
import android.widget.TextView;


/**
 * Created by Håkon S. Bøckman on 18.10.2017.
 */
/*
public class WineRecycleAdapter extends RecyclerView.Adapter<WineRecycleAdapter.WineViewHolder>{

    private static final String TAG = RecyclerAdapter.class.getSimpleName();


    @Override
    public WineRecycleAdapter.WineViewHolder onCreateViewHolder(ViewGroup parent, int viewType) {
        return null;
    }

    @Override
    public void onBindViewHolder(WineRecycleAdapter.WineViewHolder holder, int position) {

    }

    @Override
    public int getItemCount() {
        return 0;
    }

    public class WineViewHolder {

        TextView title;
        ImageView imgThumb;

        public AnimalViewHolder(View itemView) {
            super(itemView);
            title       = itemView.findViewById(R.id.animal_text);
            imgThumb    = itemView.findViewById(R.id.animal_image);
        }

        public void setData(Animal current) {
            this.title.setText(current.getTitle());
            this.imgThumb.setImageResource(current.getImageId());
        }
    }
}
*/